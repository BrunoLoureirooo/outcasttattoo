# Directus — build-time only, never shipped to the client.
# Leave unset for local dev without a Directus server: the site falls back
# to the JSON snapshots in src/data/ and renders no pieces.
DIRECTUS_URL=https://directus.example.com
DIRECTUS_TOKEN=your-static-token-with-read-only-published-role
